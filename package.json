{
  "name": "morse-tree-trainer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "http-server . -p 4173 -c-1",
    "check": "node scripts/check.mjs",
    "test:smoke": "playwright test tests/smoke.spec.js --reporter=line"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1"
  }
}
